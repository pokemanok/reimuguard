<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pokeman.reimuguard"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />
    
     <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
     <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>
     <uses-permission android:name="android.permission.GET_TASKS" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"
        >
        <activity
            android:name="com.pokeman.reimuguard.activity.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.pokeman.reimuguard.activity.MainActivity"></activity>
        <activity android:name="com.pokeman.reimuguard.info.DeviceInfo"></activity>
        <activity android:name="com.pokeman.reimuguard.utils.EnterPsd"></activity>
        <service android:name="com.pokeman.reimuguard.utils.MarisaWidgets"/>
        <service android:name="com.pokeman.reimuguard.service.WatchDogService"/>
         <!-- 拦截界面要在一个单独的任务栈中 -->
        <!-- 不需要去显示手机卫士的图标,true不要显示最近的图标,false显示最近开启应用的图标 -->
        <activity
            android:name="com.pokeman.utils.EnterPsd"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />
    </application>

</manifest>
